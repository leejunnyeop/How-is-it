spring:
  config:
    import:
      file: /run/secrets/application_secret_new1
  datasource:
    url: jdbc:mysql://portfolier.crofsnhlhw25.us-east-1.rds.amazonaws.com:3306/howisit
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        ddl-auto: none
        dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
logging:
  level:
    org:
      springframework:
        mail: DEBUG


