spring:
  datasource:
    url: jdbc:mysql://portfolier.crofsnhlhw25.us-east-1.rds.amazonaws.com:3306/howisit
    username: admin
    password: portfolier0108
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  config:
    import: file:application-secret.yml
  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
